name: ReportifyBot Run

on:
  workflow_dispatch:
  schedule:
    - cron: "0 19 * * *"   # roda todo dia às 19h UTC (opcional)

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Rodar bot (relatório + resumo + mensagens privadas)
        env:
          MY_API_REPORTFY: ${{ secrets.DISCORD_BOT_TOKEN }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          DISCORD_CHANNEL_ID: ${{ secrets.DISCORD_CHANNEL_ID }}
          DISCORD_TARGET_USERS: ${{ secrets.DISCORD_TARGET_USERS }}
          GITHUB_TOKEN: ${{ secrets.REPORTFY_GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ secrets.REPOSITORY }}
          
        run: python ReportfyBot.py
